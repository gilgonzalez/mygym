# Mismo proyecto de Supabase que apps/web — usar la URL y la anon key
# públicas (nunca la service role key acá, esto corre en el dispositivo).
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
